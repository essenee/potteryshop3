This version preserves wagtailimages.image model for images,
and authors Gallery through gallery page, using Caption field for
gallery image title/link to Product page. Potteryshop2 uses customimage model
to author this in 1 step in image (but then can't control order in Gallery).

